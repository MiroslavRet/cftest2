import { Button } from "@nextui-org/button";

export default function ButtonRefundCampaign() {
  return (
    <Button onPress={() => {}} isDisabled={false} color="warning" variant="flat">
      Refund Campaign
    </Button>
  );
}
