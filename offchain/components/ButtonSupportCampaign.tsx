import { Button } from "@nextui-org/button";

export default function ButtonSupportCampaign() {
  return (
    <Button onPress={() => {}} isDisabled={false} color="secondary" variant="shadow">
      Support Campaign
    </Button>
  );
}
